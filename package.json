{
  "name": "ds-stack",
  "version": "0.1.0",
  "description": "Simple implementation of a stack data structure",
  "scripts": {
    "test": "yarn jest"
  },
  "dependencies": {
    "@types/jest": "^24.0.15",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  }
}
